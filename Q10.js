let product = 1; // Initialize product to 1
for (let i = 1; i <= 15; i += 2) {
  product *= i; // Multiply the current odd number with the product
}
console.log("The product of odd numbers from 1 to 15 is:", product);
